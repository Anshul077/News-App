import React from "react";

function Health_NewsItem({title,description}) {
  return (
    <div>
      <h3>{title}</h3>
      <h4>{description}</h4>
    </div>
  );
}

export default Health_NewsItem;
